{
  "name": "tailwindless",
  "version": "1.0.0",
  "description": "",
  "main": "src/index.less",
  "scripts": {
    "watch": "rimraf dist/ && sass src/index.scss dist/index.css --watch",
    "build": "rimraf dist/ && sass src/index.scss dist/index.css --no-source-map --style=compressed",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "rimraf": "^3.0.2",
    "sass": "^1.45.1"
  }
}